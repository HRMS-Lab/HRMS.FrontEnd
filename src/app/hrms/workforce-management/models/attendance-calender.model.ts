export interface CalenderInfo {
    monthName: string
    month: number
    year: number
    lock: boolean
  }

  export interface CalenderPayload {
    //day: number
    month: number
    year: number
    lock: boolean
  }